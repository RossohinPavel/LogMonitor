# Docker 
COMPOSE_PROJECT_NAME="logmonitor"
BACKEND_PORT=3000
FRONTEND_PORT=5173

# Backend
DB_PATH="../data/db.sqlite"

# Frontend
VITE_BASE="" # Может быть /logmonitor/
VITE_BACKEND_URL="http://localhost:3000" # Указываем адрес без закрывающего слеша, sdk его сам подставит.